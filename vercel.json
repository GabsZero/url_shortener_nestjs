{
  "version": 2,
  "buildCommand": "npm run build && prisma generate",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}